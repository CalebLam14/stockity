{
  "name": "stockity",
  "version": "1.0.0",
  "description": "A very simple inventory tracking app.",
  "dependencies": {
    "concurrently": "^7.0.0"
  },
  "scripts": {
    "start": "npm install && concurrently --kill-others \"cd stockity-api && npm install && npm run start\" \"cd stockity-site && npm install && npm run start\""
  },
  "author": "Caleb Lam",
  "license": "ISC"
}
